*~

/.vscode

# Generated by start.sh from public/index.html.template at container start,
# based on UI_REMOVE_CERT_FEATURE -- not the source of truth, so not tracked.
/public/index.html

# scripts/ci/validate.sh compiles parseheaders.py and runs the unit tests,
# so anyone running the CI checks locally generates these.
__pycache__/
